import { VITE_BackendURL } from "../../dataconfig"


export const RequestURL = VITE_BackendURL
export const request = {
    product: {
        allproduct: '/product',
        detailes: `/product`
    }
}
